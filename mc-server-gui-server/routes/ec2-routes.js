import { Router } from "express";
// import { connectToEc2, startServer } from "../controllers/ec2-controller.js";

const ec2Router = Router();

// ec2Router.get("/connect", connectToEc2);
// ec2Router.post("/start", startServer);

export default ec2Router;
